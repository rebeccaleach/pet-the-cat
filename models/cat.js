var orm = require('../config/orm.js');

// create the code that will call the ORM functions using burger specific input for the ORM. (wtf does this even mean)

var cat = function() {

	console.log("cat.js is connected");

};

module.exports = cat;